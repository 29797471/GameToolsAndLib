<UserControl x:Class="WinCore.UnderLineUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"><!-- x:Name="self"-->
    <RichTextBox IsDocumentEnabled="True" IsReadOnly="True">
        <FlowDocument >
            <Paragraph >
                <Hyperlink TargetName = "_blank1" Click="Hyperlink_Click" >
                    <!--<Run  Text="{Binding ElementName=self,Path=Target,Mode=OneWay}"/>-->
                    <!--<Run x:Name="run" />-->
                    <Run x:Name="run" />
                </Hyperlink >
            </Paragraph >
        </FlowDocument >
    </RichTextBox>
</UserControl>
