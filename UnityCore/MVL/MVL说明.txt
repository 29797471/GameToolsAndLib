解决UI和数据关联的问题,
将UI的部分数据和逻辑数据结构关联在一起,
数据,UI相互驱动
1.LinkMethod 关联Button 按钮点击后回调关联的对象的方法
2.LinkProp 关联任一组件的某类型成员 成员改变时对应组件的属性也同时变化,如果有关联对应组件属性的改变回调,那么组件属性变化也会驱动数据成员变化
3.LinkObject 关联一个对象
4.LinkList 关联一个列表,该组件所在GameObject的子,作为模版也会挂LinkItem